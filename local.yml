- hosts : localhost
  become : true
  pre_tasks : 
    - name : Update cache
      apt : 
        update_cache : yes
      tags : 
        - nodejs
        - zsh
  tasks : 
    - include : ansible_file/zsh.yml
    - include : ansible_file/node.yml